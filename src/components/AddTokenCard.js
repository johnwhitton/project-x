import React from 'react';

const AddTokenCard = ({token}) => (
  <div className='add-token-card-container'>
    <span>
      <i className='fas fa-plus-circle' />
    </span>
    <span>Add to shell</span>
  </div>
);

export default AddTokenCard;
